{
  "name": "producto-matriz-sin-limite",
  "version": "1.0.0",
  "description": "server que realiza la multiplicación de matrices",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install" : "node-gyp configure && node-gyp rebuild",
    "start" : "nodemon index.js"
  },
  "keywords": [
    "server",
    "pi",
    "c++",
    "montecarlo",
    "serial",
    "multithread",
    "pthread"
  ],
  "author": "jhonattan vargas",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
